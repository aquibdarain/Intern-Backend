
create table student(
    id int NOT NULL AUTO_INCREMENT,
    name varchar(255) NOT NULL,
    lastName varchar(255),
    Branch varchar(255),
    college varchar(255),
    location varchar(20),
    primary key(id)
);
 